import { View, ScrollView } from 'react-native';
  
import Providers from '@/src/components/providers/providers';
import { Slot } from 'expo-router';
export default function SettingsProvidersLayout() {
  
  return (
    <Slot />
  );
}
